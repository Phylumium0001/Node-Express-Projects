const db = require("./db")

async function addUser(id, firstname, lastname, email, hash, membership_status){
  return
}

async function findUser(email){
  return
}

async function addMessage(userId,messageId, title, content,timestamp){
  return 
}
