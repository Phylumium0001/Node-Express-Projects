<%- include('header') %>

<div class="items">
    <% items.forEach(item => { %>
     <div class="item">
        <p><%= item.name %></p>
        <p><%= item.price %></p>
     </div>
    <% }) %>
</div>

<%- include('footer') %>